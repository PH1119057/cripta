# LEGACY — strategy_dispatcher candidates

**Статус:** HISTORICAL / NON-CANONICAL

Этот каталог относится к старому profile-based Dispatcher contour.

Идентификатор `M3` не имеет канонического смысла и запрещён для новой
документации/разработки.

Текущий Dispatcher V2 является strategy-agnostic context layer и не создаёт
Strategy profile/suitability.

Перед удалением/миграцией legacy-кода требуется отдельная source/runtime задача,
поскольку historical identifiers ещё встречаются в старых компонентах/tests.
