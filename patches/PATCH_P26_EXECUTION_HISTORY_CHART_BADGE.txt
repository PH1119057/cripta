PATCH P26 — EXECUTION HISTORY + FIXED DIRECTION BADGE

Base: v0.8.5 · P25
Target: v0.8.5 · P26

Changes:
1. REST backfill for /v5/execution/list (latest 50 executions for selected symbol) on startup/reconnect and periodic reconciliation.
2. WebSocket executions are merged/deduplicated with REST history instead of replacing it, so the Executions tab survives restarts/reconnects and catches missed fills.
3. LONG/SHORT chart badge is pinned near the right-hand price edge at current Mark instead of following the latest candle.
4. When read-only runtime stops, Public/Private/REST indicators are cleared instead of staying misleadingly green.
5. No mutation/risk policy changes. Atomic entry Stop/TP from P25 remains unchanged.

Operational note:
Clock-drift safety remains fail-closed. If Bybit reports excessive clock offset, synchronize Windows time before reconnecting.
